# Feuille de route et suivi des tâches

Ce fichier répond à une seule question : **où en est-on ?** Il complète les trois autres
documents du dépôt sans les recouper.

| Document | Répond à |
|---|---|
| `docs/brief.md` | Pourquoi on fait ça, et jusqu'où |
| `CLAUDE.md` | Comment on le fait |
| `docs/roadmap.md` | Où on en est |
| `docs/journal.md` | Ce qui a résisté en chemin |

## Règle de mise à jour

**Le statut d'une tâche ne change que dans la PR qui le change.** On ne tient pas ce fichier à
jour « de temps en temps » : une PR qui termine `J1-05` bascule `J1-05` en `terminé` dans le même
diff. C'est ce qui évite le suivi fantôme — un tableau qu'on met à jour séparément dérive en
trois semaines et devient plus nuisible qu'utile.

Corollaire : ce fichier est **volontairement grossier**. Une tâche = une session = une PR, comme
le veut le §12 de `CLAUDE.md`. Le détail d'exécution n'a pas sa place ici ; s'il faut discuter
d'un point, on ouvre une issue et on la référence dans la colonne *Notes*.

## Statuts

| Statut | Sens |
|---|---|
| `à faire` | Prête à être prise, aucune dépendance en attente |
| `bloqué` | En attente de quelque chose d'extérieur — la colonne *Dépend de* dit quoi |
| `en cours` | Une session travaille dessus |
| `terminé` | Mergée sur `main` |
| `reporté` | Décidée mais volontairement repoussée, avec la raison |

Le statut `bloqué` est important dans ce projet : plusieurs tâches dépendent d'une action en
console web qu'aucune session ne peut faire. Les distinguer de `à faire` évite qu'une session
prenne une tâche qu'elle ne peut pas terminer.

Les tâches marquées **[humain]** ne sont pas réalisables depuis une session cloud.

**Ne marquer `[humain]` qu'une tâche dont *aucune* part n'est réalisable en session.** Une tâche
mixte ainsi marquée est une tâche que personne ne prend : la session s'en écarte, et le porteur
croit qu'elle attend la session. Si les deux natures coexistent — un réglage de console et un
fichier versionné — on scinde en deux lignes. Constaté sur J1-03 le 5/8, resté `à faire` pendant
que sa moitié versionnée était réalisable depuis le premier jour.

---

## Jalon 1 — Bootstrap, ingestion, CI complète

Épreuve visée : la CI devient l'unique juge de qualité.

| ID | Tâche | Statut | Dépend de | Notes |
|---|---|---|---|---|
| J1-01 | Documentation socle : `CLAUDE.md`, journal, brief v1.1 | `terminé` | — | PR #1 |
| J1-02 | **[humain]** Provisionnement Neon, Netlify, Sentry, clé PSI | `terminé` | — | Secrets en place |
| J1-03 | Durcissement du dépôt : secret scanning, push protection, Dependabot, CodeQL | `terminé` | — | Moitié console **[humain]** faite le 5/8 ; moitié versionnée (`dependabot.yml`, `codeql.yml`) dans la même journée. La marque **[humain]** était trop large : cf. dette. Première récolte traitée le 5/8 (journal 005) |
| J1-04 | Bootstrap : `package.json`, Astro, TS strict, ESLint/Prettier, Vitest (2 projects + garde anti-I/O), `.env.example`, job CI rapide | `terminé` | — | Toutes les dépendances des tâches parallèles sont installées |
| J1-05 | Garde SSRF — `src/lib/fetch/` | `terminé` | J1-04 | TDD strict tenu : la table de plages a été écrite avant le code et n'a pas bougé. Résidu de rebinding DNS en dette. **Un contournement réel y a été trouvé le 18/8** et corrigé le jour même : la forme *IPv4-compatible* de RFC 4291 §2.5.5.1 (`::a.b.c.d`) n'était pas décodée, donc `::127.0.0.1` et `::169.254.169.254` passaient comme adresses publiques ordinaires. Le module traitait `::ffff:`, NAT64 et 6to4 — pas celle-là. Trouvé en suivant un mutant survivant, pas en relisant le code (journal 024) |
| J1-06 | Machine à états de résolution d'URL — `src/lib/` | `terminé` | J1-04 | `src/lib/resolve/`, quatre étages purs (`attempt`, `verdict`, `states`, `arbitrate`) et 73 tests unitaires, tous écrits avant le code. Les cas viennent des mesures de J1-07 et J1-14 (154 candidates en `http`, 138 communes multiples, 5 valeurs sans schéma), pas d'exemples imaginés. Deux règles portent le reste : un scan ne ressuscite pas un `invalide` et ne sort pas de `a_revoir` — sans elles, la ré-ingestion hebdomadaire effacerait chaque décision humaine. **Aucun consommateur** : le job qui exécute ces règles est du jalon 2 (cf. dette) |
| J1-07 | Parsers Zod + fixtures gelées (`geo.api.gouv.fr`, DILA) | `terminé` | J1-04 | `src/lib/sources/`, deux parsers, 55 tests unitaires contre fixtures verbatim. Les contraintes de schéma sont **mesurées** sur les jeux complets (34 969 communes, 35 803 mairies), pas supposées. Nouveau projet Vitest `contract` + workflow hebdomadaire qui interroge les vraies API : une fixture sans lui pourrit sans que personne le voie. A trouvé un défaut de J1-08, corrigé ici (migration `0001`) |
| J1-08 | Schéma Drizzle (5 tables du §6 du brief) + 1re migration | `terminé` | J1-04 | 5 tables verbatim, `methodology_version` `not null` et sans défaut sur `scan_run` **et** `measurement`, idempotence portée par deux index uniques, 12 `CHECK`. Migration `0000` **appliquée et éprouvée** sur un Postgres 16 embarqué dans le conteneur de session : chaque contrainte a refusé une insertion (journal 014). Le dry-run sur branche Neon est arrivé avec J1-11, le 12/8 |
| J1-09 | Coquille du site : layout accessible, en-têtes de sécurité, pages légales et méthodologie | `terminé` | J1-04 | Gabarit unique, 6 pages, CSP stricte + politique de cache par route tenues par un registre testé. Sentry câblé (serveur ; navigateur sous condition, cf. dette). axe-core sans violation sur les 6 pages, clair et sombre — mesuré en session, pas encore en CI : c'est J1-12 |
| J1-10 | **[humain]** Environment GitHub `production` + politique de branche `main` | `terminé` | — | Fait le 9/8 : environment `production`, branche `main` seule autorisée, reviewer requis, `DATABASE_URL` et `DATABASE_URL_UNPOOLED` déplacés dedans ; `NEON_API_KEY` laissé au niveau du dépôt, seul secret dont un job de PR aura besoin. `OPS_TOKEN` et `NETLIFY_AUTH_TOKEN` **retirés plutôt que tournés** : aucun code ne les lit, et un jeton sans consommateur n'est qu'une surface d'exposition. `ingest.yml` nomme l'environment dans la même PR. Statut **déclaré par le porteur** : `GET /environments` répond 403 (cf. dette) |
| J1-11 | Job CI d'intégration + branche Neon éphémère + migration en dry-run | `terminé` | J1-10 | Job `integration` sur chaque push : branche Neon éphémère, migrations en compte rendu **puis** appliquées, 17 tests d'intégration, suppression en `always()`. Règles pures dans `src/lib/neon/` (44 tests écrits avant le code) parce que `NEON_API_KEY` est un secret de dépôt qu'**aucune session ne peut lire** : le transport est intestable ici, les règles ne le sont pas. Le pruner ne touche que les noms préfixés `ci-`, jamais la branche par défaut, jamais une branche de moins de deux heures. Chaîne entière **répétée en session** contre une fausse API Neon et un Postgres jetable : création, élagage d'une branche fuitée, poll de disponibilité, migrations, 17 tests verts, suppression idempotente, plus cinq chemins rouges. **La première exécution en CI a quand même trouvé un écart** — `GET /projects` exige un `org_id` que la spécification dit facultatif — corrigé dans la même PR (journal 022). Ne donne pas de base à la deploy preview : cf. dette |
| J1-12 | Job CI E2E : Playwright + axe-core sur deploy preview, upload d'artefacts | `terminé` | J1-09 | Job `e2e` sur chaque PR : 22 tests, axe-core sans filtre de tags sur les 6 pages en palettes claire **et** sombre, plus la structure du gabarit (un seul `h1`, hiérarchie sans saut, lien d'évitement, anneau de focus, 404). Liste des pages dérivée du registre de routes, pas écrite dans la suite. Budget de 6 min appliqué par `scripts/budget.mjs` ; mesuré à 11,4 s en session. Chemins rouges éprouvés avant le push (journal 013) |
| J1-13 | **[humain]** Protection de `main` : checks requis, merge linéaire | `terminé` | J1-04 | Check `verify` exigé et historique linéaire activés le 5/8. Statut **déclaré par le porteur** : une session ne peut pas le constater (cf. dette) |
| J1-14 | Job d'ingestion du référentiel des communes | `terminé` | J1-05, J1-07, J1-08 | Logique pure dans `src/lib/ingest/` (périmètre, appariement, plan) écrite en test-first, écriture idempotente dans `src/db/ingest.ts`, job `src/jobs/ingest-communes.ts` déclenché par `.github/workflows/ingest.yml` (`workflow_dispatch`, rapport en artefact). **Exécuté pour de vrai en session** contre un Postgres jetable : 34 969 communes lues, **1 067** ingérées, 1 224 URL candidates, deuxième passage à 0 insertion (journal 017). Neuf tests d'intégration existent ; les faire tourner en CI reste J1-11 |
| J1-16 | Application des migrations à la production par workflow | `terminé` | J1-08, J1-10 | **Tâche née d'un trou de cette feuille de route**, découvert en le heurtant : J1-11 disait comment une *preview* obtient un schéma, J1-14 comment les données sont chargées, et rien ne disait comment la **production** reçoit ses migrations. Elle n'en avait reçu aucune (journal 021). `migrate.yml` (`workflow_dispatch`, environment `production`), job `src/jobs/migrate.ts`, planificateur pur en test-first (`src/lib/migrate/`, 12 tests) qui joint le journal Drizzle aux `created_at` de la base — **mesuré**, c'est la seule clé disponible, le tag n'est jamais stocké. Rend compte avant d'agir (`apply` par défaut à `false`) et **refuse** d'appliquer sur une base qui a dérivé. Éprouvé en session : base vide → 2 migrations, rejeu → 0, dérive simulée → refus, variable absente → erreur nommée |
| J1-15 | Page `/stats` minimale sur données réelles | `terminé` | J1-14 | Première page rendue depuis la base. Logique pure en test-first (`src/lib/stats/`, 20 tests), lecture en deux allers-retours (`src/db/stats.ts`), quatre tests d'intégration. Client tranché : **`pg` partout**, le SDK Neon est retiré du dépôt (cf. dette). Nouvelle politique de cache `donnees` (300 s au bord, tag `data:communes`) et un rendu dégradé qui **renonce** à son cache. **Vu sur données réelles en session** : 1 067 communes, 35 338 502 habitants, 1 224 adresses toutes `candidat`, 15 communes sans adresse (journal 019) |
| J1-17 | Workflow de tests de mutation + première mesure réelle | `terminé` | J1-04 | **Tâche née du même genre de trou que J1-16** : `pnpm test:mutation`, `stryker.config.json` et les deux dépendances étaient dans le dépôt depuis le bootstrap, le §5 promettait un cron hebdomadaire, et **aucun workflow ne les appelait** — la situation de `test:integration` avant J1-11. La première exécution a trouvé la configuration cassée deux fois : plugin jamais chargé (Stryker globe `node_modules/@stryker-mutator/*`, que pnpm remplit de liens symboliques) et clé `vitest.project` **inexistante** dans le runner, qui aurait fait tourner le projet `contract` — deux API publiques, une fois par mutant. `vitest.mutation.config.ts` isole le projet unitaire, `vitest.shared.ts` empêche la copie de diverger, un test l'assère dans les deux sens. **Mesuré : 75,57 %**, 1 957 mutants, 6 min 24 s (journal 023)
| J1-18 | Budget Lighthouse sur la deploy preview | `terminé` | J1-12 | **Troisième « le contrat le promet, rien ne l'exécute » du jalon**, après `test:integration` (J1-11) et `test:mutation` (J1-17) : le §6.4 faisait de « budget Lighthouse tenu » une condition de merge, le brief l'appelle « exigence non négociable pour un site qui publie le score des autres », et il n'y avait aucun Lighthouse dans le dépôt. Job `lighthouse` sur chaque PR, `scripts/lighthouse-budget.mjs`, deux pages (un gabarit chacune). N'assère **ni** l'accessibilité (axe-core la couvre mieux) **ni** le SEO (le `noindex` délibéré la ferait échouer). Harnais éprouvé en session contre un serveur local, vert et rouge sur les trois familles d'assertions ; **les seuils, eux, viennent de la CI** — aucune session ne peut mesurer le vrai site (journal 025) |

**Parallélisable après J1-04** : J1-05, J1-06, J1-07, J1-08 et J1-09 touchent des répertoires
disjoints et ne se gênent pas. Toutes les dépendances sont installées dès J1-04 pour qu'aucune
branche parallèle n'ait à toucher `pnpm-lock.yaml` — c'est le conflit le plus pénible à résoudre.

Le compte Netlify étant sur un plan *legacy*, le nombre de déploiements de production n'est pas
plafonné : on peut merger les PR au fil de l'eau sans surveiller un quota.

---

## Jalon 2 — Mesure sur 20 communes, fiche entité, surface d'ops

Épreuve visée : une API tierce capricieuse, l'idempotence, et le pilotage sans shell.

Vingt communes et non mille : le jalon éprouve la *boucle*, pas le volume. Le passage à l'échelle
est le jalon 3, et il n'apprend rien tant que la reprise n'est pas éprouvée sur un échantillon
qu'on peut lire en entier.

| ID | Tâche | Statut | Dépend de | Notes |
|---|---|---|---|---|
| J2-01 | Planification et reprise d'un scan — `src/lib/scan/` | `terminé` | J1-06 | Logique pure, test-first : politique (les cinq nombres et leur validation), éligibilité et échantillon, liste de travail depuis les lignes déjà écrites, conclusion d'un essai et d'un run. Le test de parcours éprouve la propriété du §8 — démarrer, interrompre, reprendre, sans doublon ni perte. 95 tests, 98,83 % de branches, **96,00 % de score de mutation** sur le module ; les survivants restants sont tous de la prose de message d'erreur (journal 027) |
| J2-02 | Client PSI : requête, parsing Zod, extraction des métriques et des `finding` | `à faire` | J2-01 | **Commence par capturer sa fixture**, ce qu'aucune session ne peut faire seule : le quota PSI sans clé est nul (mesuré, cf. journal 027) et `PSI_API_KEY` est un secret de dépôt. Le chemin est un `workflow_dispatch` sur la branche de la PR. C'est aussi cette tâche qui décide quelles pannes PSI sont transitoires — `src/lib/scan/progress.ts` prend le verdict en entrée plutôt que de le deviner |
| J2-03 | Signaux complémentaires depuis le HTML — `src/lib/signals/` | `terminé` | J1-05 | Cinq étages purs — scanner borné sur du HTML hostile, vocabulaire des trois liens de politique, en-têtes de sécurité, empreintes de CMS, agrégation aux noms des colonnes de `measurement` — et une collecte qui prend son transport par injection, donc une seule page récupérée par le client gardé (§7). **Écrit depuis un relevé, pas depuis des exemples imaginés** : 45 communes tirées du périmètre, 41 pages lues le 23/8, puis le module repassé sur ce corpus. 27 déclarations d'accessibilité, 39 mentions légales, 19 politiques de confidentialité, **aucun faux positif ni faux négatif** après vérification à la main. Décide aussi, pour ce fetch-là et pas pour PSI, ce qu'une panne vaut : le verdict qu'attendait `src/lib/scan/progress.ts`. 222 tests, 100 % de branches, **87,07 % de score de mutation** sur le module — dont 91,07 % sur le vocabulaire, qui est la partie qui décide de ce qui sera publié (journal 031) |
| J2-04 | Écriture d'un scan en base — `src/db/scan.ts` | `à faire` | J2-01, J2-02 | Le pendant impur de J2-01 : appliquer une liste de travail, prendre un bail, écrire une mesure et ses `finding`. Idempotence éprouvée en intégration sur branche Neon, pas seulement en unitaire |
| J2-05 | Job de scan et son workflow `workflow_dispatch` | `à faire` | J2-04, J2-03 | `src/jobs/scan.ts` + `.github/workflows/scan.yml`, environment `production`. Rend compte avant d'agir, comme `migrate.yml` |
| J2-06 | Surface d'ops authentifiée | `à faire` | J2-04 | Jeton porteur en en-tête, comparaison en temps constant, tout mutant en `POST`, journalisation, idempotence (`CLAUDE.md` §8). Débloque le provisionnement d'`OPS_TOKEN`, qui n'a jusqu'ici aucun consommateur |
| J2-07 | Fiche entité `/commune/[code_insee]` | `à faire` | J2-04 | Premier gabarit dynamique du site : politique de cache `donnees` et tag de purge à déclarer au registre, un parcours E2E de plus, pas un par commune (§5) |

**Ce que ce jalon ne tranche pas.** Le score composite reste une décision ouverte (`brief.md` §11)
et le schéma n'a délibérément aucune colonne pour lui. Une fiche entité affiche les signaux
mesurés et leur date, pas une note agrégée.

**Parallélisable après J2-01** : J2-02 et J2-03 touchent des répertoires disjoints. J2-06 et J2-07
attendent tous deux J2-04, mais ne se gênent pas entre eux.

---

## Jalons suivants

Non décomposés à ce stade, et c'est délibéré : détailler maintenant produirait une fausse
précision qu'il faudrait défaire. Chaque jalon est décomposé au moment de l'attaquer, à partir du
§8 du brief.

| Jalon | Livrable | Statut |
|---|---|---|
| 2 | Mesure sur 20 communes, fiche entité, surface d'ops | `en cours` — décomposé ci-dessus |
| 3 | Passage à 1 000 communes, scan par lots, reprise sur incident | `à faire` |
| 4 | Publication : classements, cache edge, purge ciblée | `à faire` |
| 5 | Méthodologie v2 appliquée à l'historique | `à faire` |
| 6 | 4 semaines sans nouvelle fonctionnalité | `à faire` |

---

## Dette et points à reprendre

Ce qu'on a laissé volontairement de côté, pour ne pas l'oublier.

| Sujet | Origine | Notes |
|---|---|---|
| Portée de `NEON_API_KEY` | 4/8 → contournée le 12/8 | Vérifier qu'il s'agit d'une clé scopée au projet et non au compte. J1-11 ne l'attend plus : le job énumère les projets et **refuse de choisir** s'il en voit plusieurs, en nommant la variable `NEON_PROJECT_ID` comme sortie. La question reste posée — une clé de compte peut créer et détruire des branches partout —, elle n'est simplement plus bloquante |
| ~~Historique non linéaire~~ | 4/8 → soldé le 5/8 | L'historique linéaire est exigé sur `main` (J1-13). Le commit de merge de la PR #1 reste dans l'historique : on ne réécrit pas `main` pour l'effacer |
| Configuration du dépôt illisible depuis une session | 5/8 | `GET /branches/main/protection` répond `403 Resource not accessible by integration`, et il en va de même des réglages de sécurité. Une session ne peut vérifier ni le §4 ni le §7 : les statuts de J1-13 et J1-03 sont **déclaratifs**. Cf. journal 002 et 003 |
| Granularité de la marque **[humain]** | 5/8 | J1-03 était marquée `[humain]` en bloc alors que la moitié (Dependabot, CodeQL) est faite de fichiers versionnés. Une tâche mixte marquée `[humain]` est une tâche qu'aucune session ne prend, donc une tâche qui dort. Relire les autres `[humain]` avec cette grille |
| CodeQL : périmètre et sévérité | 5/8 | Trois choix de départ à revoir après quelques exécutions réelles : suite de requêtes par défaut plutôt que `security-extended`, check **non requis**, et **aucune analyse des fichiers `.astro`** faute d'extracteur — la couverture des gabarits repose sur ESLint et l'E2E |
| Langue des descriptions de PR | 4/8 | Le §4 ne tranche pas, et la pratique a déjà divergé : PR #1 en anglais, PR #3 en français. À arbitrer dans le §4 avant que le corpus ne devienne illisible |
| Décisions ouvertes du brief §11 | — | Score composite, rétention au-delà de 12 mois, fréquence des scans |
| ~~Suites de tests vides qui passent~~ | 5/8 → soldé le 7/8 | `--pass-with-no-tests` retiré de `test:e2e` par J1-12 (22 tests), puis de `test:integration` par J1-14, qui lui donne ses neuf premiers tests. Plus aucune suite du dépôt ne peut passer en étant vide. La seconde moitié — **rien en CI n'exécutait** `test:integration` — est soldée par J1-11 le 12/8 |
| ~~Sentry non câblé~~ | 5/8 → soldé le 6/8 | Intégration branchée dans `astro.config.mjs`, initialisation explicite (`sentry.server.config.ts`, `sentry.client.config.ts`), scrubbing des en-têtes et des query strings en fonction pure testée (§7). Côté serveur : actif dès que `SENTRY_DSN` est présent. Côté navigateur : cf. la ligne suivante |
| ~~Le SDK Sentry navigateur coûte 48 ko gz pour surveiller zéro script~~ | 6/8 → soldé le 6/8 | **Constaté** en construisant avec un DSN factice : `PUBLIC_SENTRY_DSN` présent au build embarque 145 ko (48 ko gz) de JavaScript sur chaque page, alors que le site n'envoie **aucun** script au client. La variable *était* définie sur Netlify (contexte `all`) : le check `deploy` de la PR #26 l'a constaté sur la preview — 143 ko servis, budget 20 ko — et elle a été retirée. Réversible d'un clic le jour où un îlot existera ; l'arbitrage sera alors explicite (relever le budget dans une PR qui l'argumente). Le §2 de `CLAUDE.md` porte la mesure |
| **[humain]** `OPS_TOKEN` n'est pas marqué « secret » sur Netlify | 6/8 | **Constaté** en listant l'environnement du projet : `DATABASE_URL` et `PSI_API_KEY` sont masqués, `OPS_TOKEN` est renvoyé **en clair** par l'API. Le §7 tranche : rotation à la moindre suspicion, et la valeur a transité en clair dans une session. À faire tourner **et** à recréer avec l'option « secret » |
| **[humain]** `SENTRY_AUTH_TOKEN` doit vivre sur Netlify | 6/8 | Le §9 le rangeait dans les secrets Actions ; **constaté** que le build tourne sur Netlify, donc l'upload des source maps aussi. Sans le jeton, l'upload est désactivé par la configuration et le reste marche. Corrigé dans le §9 ; reste à poser la variable côté Netlify pour obtenir des traces lisibles |
| `pnpm approve-builds` non silencieux | 5/8 | `onlyBuiltDependencies: []` déclaré dans `pnpm-workspace.yaml`, mais pnpm 10.33 affiche quand même l'avertissement à chaque installation. Cosmétique, à revoir sur une version ultérieure |
| ~~Écriture des tests E2E~~ | 5/8 → soldé le 6/8 | `tests/e2e/` porte les parcours de J1-12. Le fichier `routes.ts` dérive la liste des pages de `ROUTE_POLICIES` plutôt que de l'écrire : une page ajoutée au registre est couverte sans que personne y pense, et une route dynamique — celle qui multiplierait la couche par le nombre de communes — n'a pas d'adresse à visiter, donc ne peut pas y entrer |
| ~~Le garde SSRF laissait passer `::127.0.0.1`~~ | 18/8 → corrigé le 18/8 | La forme *IPv4-compatible* de RFC 4291 (`::a.b.c.d`), dépréciée au profit de `::ffff:a.b.c.d` et **oubliée pour cette raison même**. `::169.254.169.254` était classé `public`. Correction : `::/96` rejoint la table d'imbrications, et `classifyAddress` juge d'abord l'adresse IPv6 **nommée** avant de décoder — sans quoi `::` et `::1`, qui vivent dans `::/96` sans être des adresses IPv4-compatibles, seraient décodés en `0.0.0.0` et `0.0.0.1` : toujours bloqués, mais le journal appellerait la loopback « unspecified ». Sept tests écrits **avant** le correctif, rouges d'abord (§5) |
| Rebinding DNS non couvert | 5/8 | Le garde résout, juge, puis rend le **nom d'hôte** à `fetch`, qui résout à nouveau. Un enregistrement dont le TTL expire entre les deux réponses peut donner une adresse publique au garde et une adresse privée à la connexion. Fermer la brèche exige de composer l'IP vérifiée avec un `Host` épinglé et un dispatcher dédié. Documenté plutôt qu'à moitié corrigé |
| Ports non restreints par le garde SSRF | 5/8 | Le garde n'impose ni 80 ni 443. Une fois les plages privées bloquées, le risque résiduel porte sur des services publics exotiques ; à réévaluer si le scan sort de la page d'accueil |
| Branches défensives et couverture | 5/8 | `noUncheckedIndexedAccess` pousse à écrire des gardes que TypeScript prouve inatteignables, et le seuil de 90 % de branches interdit de les laisser non couvertes. Les deux exigences tirent en sens inverse : la sortie est de changer la représentation des données, pas d'ajouter un test factice. Cf. journal 004 |
| ~~Plafond Dependabot atteint sur npm~~ | 5/8 → constaté le 5/8 | Hypothèse confirmée dans la minute suivant le merge : libérer le plafond a fait apparaître trois majeures d'outillage jusque-là invisibles (`typescript`, `astro-eslint-parser`, `globals`). **Un plafond atteint ne se distingue pas d'un dépôt à jour.** Cf. journal 006 |
| ~~Réglages Astro 7 à revoir dans J1-09~~ | 5/8 → soldé le 6/8 | `compressHTML` remis à `true`, et pas par prudence : sous `'jsx'`, un retour à la ligne entre un élément en ligne et le texte voisin est **supprimé** au lieu d'être réduit à une espace, alors que Prettier formate les `.astro` avec la sémantique HTML — où ce même retour *est* une espace — et reflow librement à 100 colonnes. Les deux se contredisent, et le résultat est un `méthodologiepour` produit par une passe de formatage que personne ne relit. Quant à `src/fetch.ts` : **constaté** sans objet, le module du projet est `src/lib/fetch/` |
| `git ls-remote` est disponible depuis le conteneur | 5/8 | Corrige une affirmation de l'entrée 002 du journal : résoudre une étiquette en SHA est possible sans authentification, et c'est ce qui permet de contrôler les pins d'actions tierces (§7). Une limite supposée, jamais testée, avait été inscrite comme un fait |
| ~~Advisories résiduelles — `esbuild` et `qs`~~ | 5/8 → soldé le 18/8 | Corrigées par override (`esbuild` ^0.25, `qs` ^6.15.2), avec `nanoid` ^3.3.18 qui s'était ajoutée entre-temps. **La note d'origine était fausse sur deux points**, et c'est le plus intéressant : le chiffre annoncé — 0 haute / 2 modérées — valait 4 hautes et 2 modérées au 18/8, et `qs` n'est pas « dans l'outillage Netlify » mais sous `@stryker-mutator/core`. Personne n'avait menti ; personne n'avait regardé. C'est cette ligne qui a produit `audit.yml` |
| ~~L'E2E et Lighthouse échouent sur une instabilité de plateforme, pas sur le diff~~ | 18/8 → soldé le 19/8 | Traitée par le préchauffage, pas par un seuil. `src/jobs/warm-preview.js` réveille **toutes** les routes du registre avant que quoi que ce soit ne mesure, en **étape de CI distincte** dans `e2e` et dans `lighthouse` — donc hors du budget de 6 min que le §5 donne à la couche, parce qu'un budget doit mesurer la suite et non l'humeur de la plateforme. La règle qui distingue « le site est cassé » de « la fonction se réveille » vit en logique pure dans `src/lib/preview/readiness.ts` (40 tests écrits avant le code) et sert les deux consommateurs : le job, et le `gotoReady` de la suite, qui réessaie **de façon bornée** sur 5xx et sur `ERR_ABORTED` et échoue en disant que la preview n'a pas répondu. `retries` reste à 1 : le ticket #46 l'interdit, et à raison. L'assertion de statut a désormais son propre fichier (`tests/e2e/availability.spec.ts`), pour que « l'accessibilité est rouge » ne puisse plus vouloir dire « le site n'a pas répondu ». **Chaîne répétée en session contre un serveur factice** — chaud, froid, mort, statut inattendu, hôte introuvable — et la répétition a corrigé un message qui affirmait « the deployment answered » sur un hôte qui ne résout pas (journal 029) |
| ~~`apt-get update` bloque 14 min dans `playwright install --with-deps`~~ | 19/8 → soldé le 22/8 | **Mesuré sur la PR #47**, trois occurrences sur quatre exécutions : `e2e` et `lighthouse` annulés à leur `timeout-minutes: 15`, tous deux figés après `Get:5 … noble-security InRelease`, le miroir `azure.archive.ubuntu.com` répondant `Ign:` sur les quatre dépôts. Relancé une fois : `e2e` passe en 1 min 43 s, `lighthouse` rebloque au mot près. Étranger à tout diff, et c'est le sujet — 3e forme de la dette du journal 026. Soldée par le ticket #48, avec les **deux** correctifs qu'il proposait, et ils n'ont pas le même statut : `timeout-minutes: 5` sur la seule étape d'installation ne repose sur **aucune hypothèse** — elle dure vingt-cinq secondes d'ordinaire — et fait qu'un blocage de plateforme échoue vite sous un nom lisible au lieu de manger le job ; le retrait de `--with-deps` repose, lui, sur l'hypothèse « l'image `ubuntu-latest` porte les bibliothèques de Chromium », qu'**aucune session ne peut vérifier**. Ce qui a changé depuis le journal 029 n'est pas la certitude, c'est le cadre : la PR ne défend que ces deux lignes, donc son verdict ne peut vouloir dire qu'une chose, l'échec éventuel est franc et réversible en une ligne. **Mesuré sur la PR #49**, deux runs et quatre runners : l'étape passe de ~25 s au mieux — 11 min 02 et une annulation à 15 min au pire — à **10 à 11 s**, les quatre mesures tenant dans une seconde, et l'hypothèse est vérifiée, `e2e` et `lighthouse` commençant l'un et l'autre par `chromium.launch()`. Verdict complet en 1 min 32 s (journal 030) |
| Advisories sans correctif — `image-size` ×2, `extract-zip` | 18/8 | Trois hautes dont l'advisory publie `<0.0.0` comme version corrigée : aucun bump ni override ne peut les atteindre. Toutes trois sous `@netlify/dev*`, le serveur de développement local — **0 occurrence dans `.netlify/v1/`**, l'artefact déployé, et ce dépôt n'a pas de shell pour lancer `netlify dev`. Acceptées par écrit dans `scripts/audit.mjs`, qui rougit si l'une d'elles disparaît de l'audit sans être retirée du registre |
| `/_image` : une route publique dont le site n'a aucun usage | 18/8 | Astro enregistre `/_image` que le site utilise des images ou non, et le bundle SSR embarque une copie **vendorisée** d'`image-size` avec `icns`, `jxl` et `heif` — les trois parseurs des advisories ci-dessus, invisibles à tout audit puisqu'ils ne sont pas un paquet. Non exploitable aujourd'hui : 403 sur toute source distante (allowlist vide) et aucune image locale servie. `image.service: noop` a été **essayé et retiré** — mêmes routes, mêmes parseurs, même taille de bundle : un réglage qui ne change rien mais qui se lirait comme une mitigation. À reprendre le jour où le site servira une image, ou si Astro expose un moyen de ne pas enregistrer la route |
| ~~`sharp` : CVE libvips (haute)~~ | 5/8 → soldé le 5/8 | Corrigée par un `overrides` pnpm vers `^0.35.3` (GHSA-f88m-g3jw-g9cj, quatre CVE libvips, aucun rétroportage en 0.34.x). **La note d'origine se trompait de risque** : `astro@7.1.6` déclare `^0.34.0 \|\| ^0.35.0`, la version est donc dans sa plage supportée. Le paquet réellement dépassé est `ipx@3.1.1` (`^0.34.3`), tiré par `@netlify/images` — chemin d'image CDN que ce site n'emprunte pas. Retirer l'entrée dès qu'`ipx` accepte 0.35. Cf. journal 008 |
| ~~Alias `~` non miroité dans Vitest~~ | 5/8 → soldé le 5/8 | Miroité **par projet** dans `vitest.config.ts` : un `resolve.alias` à la racine du fichier n'est pas hérité par les entrées `projects`, chacune étant une config Vite à part entière. La version racine ne résout donc rien tout en ayant l'air correcte — piège vérifié, pas déduit. Tenu par `tests/unit/path-alias.test.ts`, qui compare la liaison obtenue par l'alias à celle obtenue par chemin relatif. Cf. journal 007 |
| Montées de TypeScript plafonnées à 6.0.x | 5/8 | `typescript-eslint@8.66.0` déclare le pair `>=4.8.4 <6.1.0` et `@astrojs/check@0.9.10` accepte `^5 \|\| ^6`. TS 7 est déjà en `latest` mais casserait les deux. Le déblocage viendra de `typescript-eslint` 9, pas de nous : ne pas confondre « aucune PR Dependabot » avec « à jour ». Cf. journal 006 |
| ~~`projectService` ignoré sur les fichiers `.astro`~~ | 5/8 → soldé le 6/8 | Réévalué à l'arrivée des gabarits, et la dégradation était pire que « silencieuse » : les expressions de template résolvent au type `error`, que la famille `no-unsafe-*` signale comme une violation. Chaque `{items.map(...)}` d'une page était rouge, et aucune réécriture ne satisfait une règle qui lit un type qu'on n'a pas su calculer. Les règles typées sont désormais **explicitement** coupées sur `**/*.astro`, avec la raison en commentaire ; `astro check`, qui passe le vrai serveur de langage sur ces fichiers et fait partie de `pnpm verify`, reste le juge des types. Tout ce qui ne demande pas de types — dont l'ensemble jsx-a11y — continue de tourner |
| ~~`astro-eslint-parser` en dépendance directe~~ | 5/8 → soldé le 5/8 | Déclarée au bootstrap mais jamais utilisée : `eslint.config.js` passe par `astro.configs.recommended`, et c'est la copie d'`eslint-plugin-astro` (`^1.3.0`) qui parse. La PR Dependabot qui proposait de la monter en 3.0.0 a rendu l'incohérence visible ; la dépendance a été retirée plutôt que montée |
| ~~Netlify ne construit pas le projet : il publie la racine du dépôt~~ | 5/8 → soldé le 5/8 | `netlify.toml` versionné (`command = "pnpm build"`, `publish = "dist"`). Vérifié sur la deploy preview de la PR #21 : `/` répond **200** avec le `<h1>` attendu, `/package.json` et `/CLAUDE.md` répondent enfin **404**, et l'horodatage rendu par `index.astro` change entre deux requêtes (`cache-status: fwd=miss`) — c'est bien la fonction SSR qui répond. Aucun réglage d'interface n'écrasait le fichier, contrairement à ce qu'on redoutait. Cf. journal 008 |
| ~~Aucun check ne regarde ce que la preview sert~~ | 5/8 → soldé le 5/8 | Job `deploy` dans `ci.yml` : `scripts/resolve-netlify-url.mjs` lit l'URL depuis le statut de commit Netlify (jamais écrite en dur, §6), `scripts/check-deploy.mjs` exige un 200, un `text/html`, le `<h1>` attendu, et surtout que `/package.json`, `/CLAUDE.md` et `/src/pages/index.astro` **ne soient pas servis** — l'assertion qui encode la panne du 5/8. Les deux chemins ont été éprouvés localement, vert sur la preview réelle et rouge sur un déploiement inexistant. Cf. journal 010 |
| ~~**[humain]** Rendre le check `deploy` requis sur `main`~~ | 5/8 → soldé le 6/8 | `deploy (the preview serves the application)` est exigé sur `main`. Statut **déclaré par le porteur** : une session ne peut pas le constater (`GET /actions/variables` et la protection de branche répondent tous deux 403). Même réserve que J1-13 |
| ~~Le check `deploy` ne couvre pas la production~~ | 6/8 → soldé le 6/8 | Job `deploy-production` sur les pushes vers `main`, hôte lu depuis la variable Actions `SITE_URL` (le §6 le garde hors du diff). Netlify ne publiant pas de statut pour la production — mesuré sur `7e36ec2` — il n'y a rien à résoudre : la variable est la seule source. Le job affirme que **la production sert l'application**, pas qu'elle sert déjà ce commit : Netlify bascule atomiquement et le déploiement peut être en vol. C'est « la production est cassée » qu'on veut attraper |
| Valeur de `SITE_URL` invérifiable depuis une session | 6/8 | `GET /actions/variables` répond 403, comme la protection de branche. Le job `deploy-production` échoue bruyamment si la variable est vide, mais **rien ne peut vérifier qu'elle pointe le bon hôte** : elle doit valoir l'origine Netlify tant que `observatoire-web.fr` n'a aucun enregistrement DNS. Troisième réglage de console de la même famille (cf. journal 003) |
| ~~Domaine mort en dur dans le code~~ | 6/8 → soldé le 6/8 | Le repli est retiré : sans `SITE_URL`, `Astro.site` reste indéfini et le gabarit **n'émet aucune URL canonique**. Une canonique absente dit « je ne sais pas » ; une canonique fausse affirme une adresse qui ne répond pas. `.env.example` ne porte plus la valeur non plus — ce fichier interdit les valeurs et en portait une |
| `noindex` sur tout le site | 6/8 | Le gabarit émet `noindex, follow` sur chaque page tant qu'aucune mesure n'est publiée : indexer un observatoire vide sous un nom qui sonne institutionnel est exactement la confusion que le §9 du brief cherche à éviter. À lever au jalon 4, en même temps que la publication — et il faudra une `SITE_URL` valide au même moment, sans quoi les canoniques resteront absentes |
| **[humain]** Mentions légales incomplètes | 6/8 | La page nomme l'éditeur (DG-Tech, dg-tech.dev) et l'hébergeur (Netlify, Inc.) mais **sans adresse postale ni identifiants d'entreprise**. Choix délibéré : une adresse inventée est pire qu'une adresse absente, et rien dans la session ne permet de la vérifier. À compléter par le porteur (raison sociale, SIREN, adresse, adresse de contact) |
| **[humain]** Aucune adresse de contact sur le domaine du site | 6/8 | Le droit de réponse renvoie vers `dg-tech.dev/#contact` et vers le dépôt public — les deux existent (**constaté** : 200 sur `dg-tech.dev`, l'ancre `#contact` figure dans sa page d'accueil). Une adresse du type `contact@observatoire-web.fr` serait plus lisible pour une commune, mais le domaine n'a aucun enregistrement DNS : l'annoncer serait annoncer une boîte qui n'existe pas |
| Cache CDN volontairement court | 6/8 | Les pages éditoriales sont en `s-maxage=3600` au bord, pas les durées « beaucoup plus longues » du §10, parce que **la purge par tag n'existe pas encore** (jalon 4) : tant qu'elle manque, l'expiration est le seul moyen de corriger une page, et une heure est le délai qu'on accepterait pour rectifier une mention légale. Les tags sont déjà émis, pour que les réponses mises en cache avant la purge soient ciblables |
| ~~Le check `deploy` de production court après la bascule Netlify~~ | 6/8 → soldé le 6/8 | **Constaté** au merge de la PR #26 : le job assère le `<h1>` et les en-têtes de *ce* commit alors que son commentaire prétendait ne vérifier que « la production sert l'application ». Netlify basculant atomiquement, il a mesuré la version précédente et viré au rouge sur une production saine. Le script attend désormais que le titre attendu soit celui qui répond. Deuxième défaut trouvé dans la foulée : un `fetch` sans `catch` faisait planter le script sur un `ECONNRESET` au lieu de conclure. Cf. journal 012 |
| Le check `deploy` suit les liens de l'accueil | 6/8 | La liste des pages vérifiées est extraite des liens internes de `/` plutôt qu'écrite dans le script — elle se tient donc à jour toute seule, mais une page qui ne serait liée depuis nulle part échapperait au contrôle. Acceptable tant que la coquille tient dans un pied de page ; à revoir quand les fiches par commune arriveront |
| ~~**[humain]** Rendre le check `e2e` requis sur `main`~~ | 6/8 → soldé | **Constaté par le porteur le 12/8** : il l'était déjà, et la ligne de dette disait le contraire. Une dette qu'on ne peut pas vérifier depuis une session (403 sur la protection de branche) se périme sans prévenir — c'est la même famille que J1-13 et J1-03 |
| `pnpm dev` sert le site sans CSS | 6/8 | **Constaté** en mettant au point J1-12 : Astro sert la feuille de style en ligne en développement, et `style-src 'self'` la bloque — page entièrement sans style, sans un mot dans le terminal. Le build n'a pas le problème (`inlineStylesheets: 'never'`). Conséquence retenue plutôt que corrigée : l'E2E juge la preview, jamais un serveur local. Corriger le développement demanderait de relâcher la CSP hors production, c'est-à-dire de faire diverger l'environnement d'observation de celui qu'on livre — ce que cette entrée vient de démontrer coûteux. Cf. journal 013 |
| E2E : un seul navigateur, une seule taille d'écran | 6/8 | La suite tourne sur Chromium en `Desktop Chrome`. Ni Firefox, ni WebKit, ni viewport mobile : le §5 borne cette couche à 5–8 parcours et un moteur, et rien ne serait mesuré de plus sur du HTML sans script. Ce qui échappe réellement au contrôle est le **reflow** (WCAG 2.2 AA, 1.4.10), qui n'est vérifié à aucune largeur. À reprendre quand une page portera des tableaux de classement |
| ~~E2E : un flake au premier passage en CI~~ | 6/8 → soldé le 19/8 | **Constaté** sur la PR #28, exécution inaugurale : `/droit-de-reponse` en palette claire a échoué sur `page.goto` (`net::ERR_ABORTED`, délai de 30 s dépassé) puis passé au réessai. La cause supposée — fonction SSR froide sollicitée par deux workers sur un déploiement qui vient de naître — était la bonne, et la ligne disait « à reprendre si le motif se répète » : il s'est répété le 18/8, en rouge et sur deux jobs. Soldée avec la dette ci-dessus, et par ce qu'elle demandait : rendre la fréquence visible plutôt que bouger un seuil — le job de préchauffage journalise chaque tentative et chaque route qu'il a dû réessayer |
| Colonne de score composite volontairement absente | 6/8 | `measurement` stocke les **signaux** (4 scores de catégories, 6 métriques, 8 signaux complémentaires) et **pas** de score agrégé : sa formule est une décision ouverte du §11 du brief, et une colonne nullable ajoutée « en attendant » l'aurait tranchée à moitié. À ajouter par la PR qui arbitre la formule, avec sa migration — c'est un exercice de plus pour l'épreuve 2, pas un coût |
| Migrations sans chemin de retour | 6/8 | `drizzle-kit` ne génère pas de migration descendante. La `0000` est trivialement réversible (cinq `DROP TABLE`) et le point ne coûte rien tant que la base est vide, mais la §6.5 de `CLAUDE.md` exige « réversible **ou** explicitement documentée comme non réversible » : à trancher comme convention avant la première migration destructive, pas au moment où elle se présentera |
| Postgres 16 est disponible dans le conteneur de session | 6/8 | **Constaté** en livrant J1-08 : `psql` et `/usr/lib/postgresql/16/bin/` sont préinstallés, et un cluster jetable (`initdb` sous un utilisateur non root, `pg_ctl -o '-p 55432'`) permet d'appliquer une migration et d'éprouver ses contraintes sans branche Neon. Corrige l'hypothèse implicite selon laquelle une migration ne pouvait être exécutée qu'en CI. Ne remplace ni Neon ni les données réelles (épreuves 2 et 5) — c'est un moyen de mise au point, pas un juge. Cf. journal 014 |
| `updated_at` maintenu côté écrivain | 6/8 | `.$onUpdate()` de Drizzle, donc respecté par les écritures qui passent par Drizzle et par elles seules. Un `UPDATE` en SQL brut laisserait la colonne mentir. Un trigger Postgres serait la vraie réponse ; il coûte une migration de plus et n'a pas de client tant qu'aucun job n'écrit. À reprendre au jalon 2, quand le scan écrira réellement |
| ~~Le périmètre v1 mesuré est de **1 067 communes**, pas 950–1 000~~ | 6/8 → soldé le 7/8 | Le §3 du brief annonce « de l'ordre de 950 à 1 000 entités » **et** dit que le comptage exact se dérive de l'API. Compté sur le référentiel complet le 6/8 : **1 067** communes de plus de 10 000 habitants, dont 2 sans aucune fiche mairie dans l'annuaire (`49126`, `98747`). Écart de ~7 % : sans conséquence technique, mais c'est un chiffre qui finira sur une page publique. J1-14 tranche les deux moitiés : le chiffre est **dérivé** à chaque exécution et publié dans le rapport du job — jamais écrit dans le code — et le porteur a arbitré le même jour en faveur de la mesure. Brief v1.4 (§3, §4, §7) et page méthodologie citent 1 067 avec sa date |
| Un contrat rouge n'est visible que dans Actions | 6/8 | Le workflow `contracts.yml` échoue bruyamment au sens de la CI, mais un cron rouge ne se voit que dans l'onglet Actions et dans l'email de notification GitHub. Personne ne regarde l'onglet Actions un lundi matin. Ouvrir une issue automatiquement demanderait `issues: write` sur un workflow planifié ; à trancher si un contrat casse réellement, plutôt que d'ajouter une permission pour un cas qui ne s'est pas produit |
| ~~`commune_population_positive` rejetait six communes réelles~~ | 6/8 → soldé le 6/8 | **Constaté** en capturant les fixtures de J1-07 : Beaumont-en-Verdunois, Bezonvaux, Cumières-le-Mort-Homme, Fleury-devant-Douaumont, Haumont-près-Samogneux et Louvemont-Côte-du-Poivre, détruites en 1916 et jamais reconstruites, sont toujours des communes et déclarent **0 habitant**. La contrainte `> 0` de J1-08 était écrite depuis une intuition de ce qu'est une commune. Migration `0001`, `>= 0`, appliquée et éprouvée en session. Hors périmètre v1 (le seuil est 10 000), donc sans effet aujourd'hui — mais la contrainte disait quelque chose de faux sur le domaine |
| Rien ne distingue « CI rouge » de « CI absente » | 6/8 | **Constaté** pendant une panne majeure de GitHub Actions (incident du 6/8, 15:22 UTC) : `verify` et `deploy` sans runner pendant 13 min, API d'annulation en 502, puis `verify` tué par son `timeout-minutes: 10`. Vingt minutes de diagnostic dans le diff avant de penser à `githubstatus.com`. Le §5 a pensé au rouge qu'on apprend à ignorer, pas à l'absence de verdict. Deux pistes : lire l'état de l'incident amont, et surtout ne pas laisser un budget de temps prononcer un verdict sur un job qui **attendait un runner** au lieu de travailler — l'API expose la différence (`started_at` du job contre celui des étapes). À traiter après la panne, pas pendant. Cf. journal 016 |
| Révision Chromium du conteneur ≠ révision épinglée | 6/8 | La session embarque Chromium 1194, `@playwright/test@1.62.1` épingle 1234, et `playwright install` est déconseillé dans cet environnement. Sans conséquence en CI, qui installe la révision épinglée ; en session, valider la suite demande un `executablePath` vers le binaire préinstallé. À reposer si une divergence de rendu apparaît un jour entre la session et la CI |
| Les jobs sont compilés par `tsc` dans `dist-jobs/` | 7/8 | **Constaté** : le *type stripping* de Node 22.22 ne résout pas un spécificateur `./x.js` vers un fichier `.ts`, et tout le dépôt écrit ses imports ainsi. Un job ne peut donc pas être lancé sur ses sources. Les deux issues étaient un exécutable TypeScript en dépendance (`tsx`) ou une passe `tsc` ; la seconde a été retenue — zéro dépendance de plus, et `build:jobs` entre dans `verify`, donc une erreur de résolution casse la PR au lieu d'attendre le jour du déclenchement. À revoir si Node apprend à résoudre les `.js` |
| Fréquence d'ingestion non tranchée | 7/8 | `ingest.yml` n'a **pas** de `schedule:`. Le référentiel bouge une fois par an, l'annuaire en continu, mais la fréquence est une décision ouverte (§11 du brief) et un cron « en attendant » la trancherait par accident. À décider quand le jalon 2 dira ce que coûte une re-mesure |
| `geo.api.gouv.fr` limite le débit à quelques téléchargements complets | 7/8 | **Constaté** en session : trois tirages du référentiel complet (4,2 Mo) en cinq minutes, et l'API répond `503` sur les quatre tentatives suivantes. Sans conséquence en production — un tirage par exécution — mais c'est la raison pour laquelle une mise au point locale doit travailler sur une capture, pas sur l'API. Le job a réagi correctement : quatre essais, backoff, puis un échec explicitement étiqueté « disponibilité » et non « dérive de contrat » |
| ~~Le SDK `@neondatabase/serverless` est installé et inutilisé~~ | 7/8 → soldé le 9/8 | Tranché par J1-15, la PR qui lit la base depuis une page : **`pg` partout**, et la dépendance retirée. `@astrojs/netlify` déploie le point d'entrée SSR en *fonction* Netlify — un processus Node sur Lambda, pas un runtime edge —, donc il tient une socket, et l'endpoint *pooled* de Neon existe exactement pour cette forme. Le SDK n'apportait rien et donnait deux clients, deux modes de panne et deux montées de version à suivre. Le pool est mémoïsé par conteneur (`max: 1`) et jamais fermé : c'est la plateforme qui recycle le conteneur |
| Les URL candidates ne sont jugées par personne | 7/8 → moitié faite le 9/8 | L'ingestion enregistre 1 224 candidates en `candidat`, verbatim : 154 en `http`, des liens profonds vers « mes démarches », et 138 communes en portent plusieurs. J1-06 livre les **règles** qui les jugent (`src/lib/resolve/`) ; il manque le **job** qui les exécute, c'est-à-dire celui qui requête et écrit — jalon 2. La file d'attente est donc décrite mais toujours pleine, et `site` contient encore des valeurs dont on sait que certaines ne répondront pas — c'est voulu : un rejet doit laisser une trace, donc il doit exister une ligne à rejeter |
| Rien ne stocke l'URL élue d'une commune | 9/8 | `arbitrateCommune` désigne l'URL à mesurer parmi les candidates vérifiées, et **rien ne l'enregistre** : `site` n'a pas de colonne disant « c'est celle-ci ». Aujourd'hui l'information est recalculable (une seule `verifie` par commune une fois l'arbitrage appliqué), donc la colonne serait redondante ; elle cessera de l'être le jour où une commune gardera deux `verifie` — le cas `several-verified`, qui attend une décision humaine. À trancher au jalon 2, avec la surface d'ops qui affichera cette file, pas avant : une colonne ajoutée « en attendant » trancherait à moitié, comme l'aurait fait le score composite |
| Un site qui répond depuis un autre hôte est signalé, pas arbitré | 9/8 | `judgeObservation` rend `movedHost` quand la chaîne de redirections finit ailleurs que sur l'hôte demandé, et n'en tire **aucune conséquence** : l'état reste `verifie`. Décider qu'un tel déplacement est suspect demanderait de savoir à quelle fréquence il se produit, et le §5 interdit toute requête sur le chemin d'une PR — le chiffre n'existera qu'après le premier scan réel. Écrire un seuil avant de l'avoir mesuré est exactement ce que ce dépôt reproche ailleurs (cf. `commune_population_positive`). À reprendre au jalon 2, avec la mesure |
| ~~`UrlCheck` n'est pas une union discriminée~~ | 9/8 → soldé le 18/8 | Deux membres : une acceptation qui porte une `URL`, un refus qui porte `reason` et `detail`. Le compilateur a désigné chaque appelant qui s'appuyait sur la forme lâche, et **quatre branches mortes** ont disparu — le compte passe de 636 à 632 et la couverture de 94,96 % à 95,25 %, sans un test de plus. C'est la sortie que réclamait la ligne « branches défensives et couverture » |
| `/stats` répond 200 même quand la base est muette | 9/8 | Choix argumenté, pas un oubli : `scripts/check-deploy.mjs` suit les liens de l'accueil et exige un 200 sur chacun, donc une page qui répondrait 503 faute de base ferait virer au rouge un check pour une raison étrangère au diff — précisément la CI qu'on apprend à ignorer (§5). La contrepartie est réelle : **rien en CI ne distingue « preview sans base » de « chemin de lecture cassé »**. Trois atténuations aujourd'hui — la lecture ratée est journalisée côté serveur, la réponse dégradée renonce à son cache (donc l'incident ne se fige pas au bord), et le test E2E du tableau se **saute** visiblement plutôt que de passer à vide. La vraie réponse est une sonde qui connaît l'environnement qu'elle interroge : elle appartient à la surface d'ops (jalon 2) |
| Une deploy preview n'a aucune base — donc `/stats` y est toujours dégradée | 9/8 | La question posée était : la preview lit-elle la base de production, ce que le §7 interdit ? **Mesuré sur la preview de la PR #33** : la page rend l'état `not-configured` (« cet environnement n'est relié à aucune base de données ») et répond `no-store`. Aucun `DATABASE_URL` n'atteint le contexte *deploy preview*, le §7 est donc tenu — par la configuration, qu'aucune session ne peut lire, pas par un contrôle. La conséquence est à retenir : **aucune PR ne voit `/stats` afficher des chiffres**, et le test E2E du tableau se saute à chaque fois. **Correction du 12/8 : J1-11 ne règle pas ce point**, contrairement à ce que cette ligne annonçait. Sa branche éphémère vit dans un runner Actions et meurt avec lui ; la preview est construite par Netlify, qui n'en sait rien. Les relier demanderait de poser une variable d'environnement Netlify par PR depuis Actions — donc un `NETLIFY_AUTH_TOKEN` valant pour tout le compte (§9), pour une base détruite avant que quiconque ouvre la page. À reprendre au jalon 2 avec la sonde d'ops, ou pas du tout |
| Une lecture dégradée n'atteint pas Sentry | 9/8 | `loadStats` attrape l'erreur pour rendre la page en 200 ; l'intégration Sentry ne capture que ce qui remonte non attrapé. L'incident n'existe donc que dans les logs de la fonction Netlify. Un `captureException` explicite est trois lignes, mais il importerait le SDK serveur dans le chemin de rendu d'une page qui n'en dépend pas encore : à faire quand un deuxième endroit aura le même besoin, plutôt que d'ouvrir cette porte pour un appelant |
| ~~La production a une base, mais pas de schéma~~ | 9/8 → confirmé le 11/8, outillé le 11/8 | **Confirmé** par le premier dispatch d'`ingest` : `relation "commune" does not exist` (SQLSTATE 42P01), message reproduit à l'identique en session contre un Postgres vide. Ce n'était donc ni les identifiants ni le réseau. J1-16 livre le workflow qui applique le schéma ; **reste à le dispatcher** pour que la production cesse d'être vide |
| ~~Le message d'erreur d'un job perdait sa cause~~ | 11/8 → soldé le 18/8 | `describeErrorChain` est câblé dans l'ingestion **et** dans `/stats`, plus seulement dans la migration. La page qui promet à un visiteur que « l'incident est journalisé » tient enfin la moitié de promesse qui lui restait |
| Le journal de la fonction Netlify est illisible depuis une session | 9/8 | `/stats` dit « l'incident est journalisé côté serveur », et **personne ici ne peut ouvrir ce journal** : les outils Netlify disponibles exposent les déploiements, pas les logs d'exécution des fonctions. Une page qui renvoie vers une trace que ses auteurs ne peuvent pas lire tient sa promesse à moitié. Deux issues, à trancher au jalon 2 : capturer l'erreur dans Sentry (cf. l'entrée voisine), ou faire dire à la surface d'ops ce que la dernière lecture a répondu |
| Les en-têtes `Netlify-*` de cache ne sont pas observables | 9/8 | **Constaté** par un E2E rouge sur la preview de la PR #33 : `Netlify-CDN-Cache-Control` et `Netlify-Cache-Tag` sont des instructions au CDN, que la plateforme applique puis **retire** — aucun client ne les voit. Le serveur de développement les relaie, ce qui rend la vérification en session trompeuse. Conséquence retenue plutôt que contournée : l'unitaire assère ce qu'on **émet**, l'E2E assère ce que le bord **en fait** (`Cache-Status`, RFC 9211 : `stored` sur l'éditorial, rien sur une réponse dégradée). Corollaire à garder en tête pour le jalon 4 : **rien depuis l'extérieur ne peut vérifier qu'un tag de purge a bien été posé** — seule la purge elle-même le dira |
| `fetch` de Node ne passe pas par le proxy du conteneur | 9/8 | **Constaté** en voulant rejouer l'ingestion en session : `curl` télécharge les 4,2 Mo du référentiel, `fetch` reçoit `503 upstream connect error` sur la même URL — Node n'honore pas `HTTPS_PROXY`. Aucune conséquence en CI (les runners Actions sortent directement), mais **aucun job du dépôt ne peut télécharger quoi que ce soit depuis une session**. Contournement : capturer avec `curl`, puis appeler les parsers et l'écriture du dépôt sur le fichier. Corrige au passage l'entrée précédente qui attribuait ces 503 au débit de `geo.api.gouv.fr` : la limite de débit existe (mesurée le 7/8), mais ce n'est pas elle qu'on rencontrait ici. **Même limite pour Chromium** : lancé par Playwright depuis une session, il répond `ERR_CONNECTION_RESET` sur une URL publique. Une suite E2E ne peut donc être mise au point en session que contre `localhost` — la juger sur la preview est le travail de la CI, et d'elle seule |
| ~~`pg` avertit que `sslmode=require` deviendra plus faible en v9~~ | 12/8 → corrigé le 18/8 | `src/lib/db/sslmode.ts` promeut `prefer`, `require` et `verify-ca` en `verify-full`, au seul point de connexion du dépôt. Volontairement étroit : n'ajoute pas `sslmode` là où rien n'en demande — le Postgres jetable d'une session ne parle pas TLS — et ne force pas un `disable` écrit exprès. Huit tests écrits avant le module, deux chemins rouges éprouvés |
| La branche éphémère a confirmé que la production a bien son schéma | 12/8 | **Mesuré** à la première exécution verte : le compte rendu de migration sur une branche fraîchement créée dit `applied: 2, pending: []`. Une branche Neon naît par copie sur écriture de la branche par défaut, donc ces deux migrations sont celles de la production. C'est la confirmation que l'entrée 021 attendait — « je n'écrirai pas ici que la production est réparée, elle le sera quand le job l'aura dit » — obtenue par un job qui ne cherchait pas cela |
| ~~**[humain]** Rendre le check `integration` requis sur `main`~~ | 12/8 → soldé le 12/8 | Coché par le porteur dans la foulée du merge de J1-11. Les cinq checks du §5 sont désormais exigés sur `main` |
| ~~Le job Neon n'a jamais parlé à Neon~~ | 12/8 → constaté le 12/8 | `NEON_API_KEY` est un secret de dépôt : une session ne peut ni le lire ni l'utiliser. La chaîne a donc été répétée contre une **fausse** API Neon, et la première exécution en CI a trouvé l'écart en trente secondes : `GET /projects` répond `400: org_id is required` parce que le compte appartient à une organisation. La spécification OpenAPI présente `org_id` comme un **filtre facultatif** et ne dit nulle part quand il cesse de l'être — aucune lecture n'aurait donné l'information. Corrigé : découverte de l'organisation puis énumération filtrée, déclenchée par le **statut** et non par le texte du message. La fausse API répond désormais comme la vraie (journal 022) |
| Les suites d'intégration migrent à travers l'endpoint *pooled* | 12/8 | Le job applique les migrations sur l'endpoint direct, puis la suite tourne sur `DATABASE_URL` — le *pooled*, pour éprouver la forme que le site emploie (§9). Or les trois fichiers de `tests/integration/` appellent `migrate()` eux-mêmes au démarrage. Sans rien en attente, cela se réduit à du DDL `if not exists` et un `select`, qu'un pooler en mode transaction sert sans broncher ; le point n'a été **vérifié que contre un Postgres direct** en session. Si la première exécution en CI casse là, c'est la piste à suivre avant toute autre |
| `--api-base` est une porte laissée ouverte | 12/8 | Le job accepte `--api-base` pour qu'on puisse le répéter contre un serveur local ; le workflow ne le passe jamais. C'est la couture qui rend le job éprouvable, et c'est aussi un pointeur d'API modifiable par quiconque peut modifier `ci.yml` — c'est-à-dire par quiconque peut déjà tout modifier. Retenu à ce titre, à retirer si la couture cesse de servir |
| Fenêtre de fuite d'une branche éphémère | 12/8 | Entre la création de la branche et l'écriture de son identifiant dans `$GITHUB_ENV`, un runner tué ne laisse rien à l'étape de nettoyage. L'élagage au début de chaque exécution ferme la fenêtre — toute branche `ci-` de plus de deux heures est supprimée — mais **par la suivante**, pas tout de suite : un dépôt sans activité pendant une semaine garderait sa branche fuitée jusqu'à la PR d'après. Sans conséquence tant que le plafond de branches n'est pas atteint |
| La branche éphémère hérite des données de la branche par défaut | 12/8 | Neon branche par copie sur écriture depuis la branche par défaut : la base de test naît donc avec ce que la production contient. Les suites d'intégration le savent et tronquent `commune ... cascade` au démarrage — sur **leur copie**, jamais sur la production. C'est aussi ce qui donne son sens à l'exigence du §6.5 : la migration en compte rendu s'exécute contre des données réelles, pas contre une base vide |
| ~~Le garde SSRF est le plus bas score de mutation du dépôt~~ | 12/8 → **constat faux, corrigé le 18/8** | La ligne disait « une liste de plages peut être vidée sans qu'un test le remarque ». **C'est faux, et vérifié à la main** : vider `['10.0.0.0/8', 'private']` fait échouer *toute* la suite, remplacer la chaîne par n'importe quoi aussi. Ces mutants survivent parce qu'ils sont **statiques** — les tables de plages sont analysées au chargement du module par le parseur que le garde utilise, donc tout mutant dans `parseIpv4`/`parseIpv6` s'exécute à l'import, et Vitest ne réimporte pas le module entre deux mutants. Ils sont **inertes** : Stryker les exécute, ils ne peuvent pas être tués. Le 49,16 % ne mesurait pas la faiblesse des tests, il mesurait cette limite d'outil (journal 024) |
| Le code exécuté à l'import n'est mesuré par aucun test de mutation | 18/8 | Conséquence assumée d'`ignoreStatic` : les tables de plages du garde SSRF, le registre de routes et les constantes du dépôt sortent de la mesure. Elles ne sont pas pour autant sans filet — **vérifié à la main** : les vider ou les corrompre casse la suite entière au chargement, parce que le module refuse de se construire. C'est un filet plus grossier qu'un score de mutation, et c'est le seul disponible tant que le couple Stryker/Vitest ne réimporte pas les modules. À revoir si Stryker gagne l'isolation par mutant |
| 41 % des mutants survivants sont des chaînes de caractères | 12/8 → toujours vrai le 18/8 | De la prose de message d'erreur qu'aucun test n'assère mot pour mot. Le score **global** est donc en partie une mesure de « assères-tu tes phrases ». Deux issues, à trancher avant que le seuil ne devienne bloquant au jalon 5 : exclure cette classe de mutants, ou accepter que le chiffre global ne se lise pas seul. Piloter à la hausse du global sans trancher produirait exactement les tests de complaisance que le §5 refuse ailleurs |
| ~~Les mutants statiques coûtent l'essentiel du temps~~ | 12/8 → **arbitrage inversé le 18/8** | J'avais refusé `ignoreStatic` en écrivant qu'il « retirerait de la mesure la table de plages du garde SSRF ». **L'argument était faux** : cette table n'est déjà pas mesurée, ses mutants étant inertes (ligne ci-dessus). Les 509 mutants statiques du dépôt — 26 % du total — consomment 91 % du temps et ne peuvent **jamais** être tués, quelle que soit la qualité des tests : ils plafonnent le score et le rendent illisible. `ignoreStatic: true` est activé. Le score devient honnête et le cron beaucoup plus court. Ce qui reste vrai du constat initial : personne ne mesure ce code-là, et c'est désormais **dit** plutôt que noyé dans un pourcentage |
| Aucun seuil de mutation n'est bloquant | 12/8 | `break: null` dans `stryker.config.json` : le §5 veut la couche informative jusqu'au jalon 3, bloquante sur le scoring au jalon 5. Conséquence assumée à retenir : ce cron ne peut virer au rouge **que** si Stryker lui-même échoue, donc une chute du score ne réveillera personne — même angle mort que le cron des contrats, déjà en dette. La réponse est un seuil, pas une alerte, et elle attend le module de scoring |
| `pnpm preview` ne fonctionne pas | 18/8 | **Constaté** en cherchant à mesurer le site en session : `@astrojs/netlify` refuse la commande (« the adapter does not support the preview command »). Le §3 la listait comme contractuelle depuis le bootstrap. Elle est marquée inopérante plutôt que retirée — un script supprimé en silence est un script qu'une session réinvente. Conséquence : **aucune session ne peut servir le build**, donc aucune ne peut voir ce que le site rend. Sortie possible le jour où ça gêne : un petit serveur qui importe l'entrée SSR, ou `netlify dev` |
| Les seuils Lighthouse sont provisoires | 18/8 → **le Speed Index de `/` est expliqué le 19/8** | L'hypothèse de la fonction SSR froide est **vérifiée par la mesure** (PR #47) : sur une preview préchauffée, `/` passe de 2 585 ms de *speed index* et 0,94 de performance à **245 ms et 1,00**, et `/stats` de 6 659 ms — l'échec de budget du 18/8 — à **260 ms**. Facteur 10 sur l'une, 25 sur l'autre, sans une ligne de code applicatif : le coût n'appartenait pas à `/`, il appartenait à **la première route touchée**. Les seuils, eux, restent provisoires et c'est maintenant un autre sujet : mesurés à chaud, `/` et `/stats` tiennent 245 et 260 ms contre un plafond de 3 400 ms, donc le budget ne mord plus sur rien. Le resserrer est une décision à argumenter dans une PR (§6.3), pas un chiffre à ajuster en passant — et elle demande plus d'une exécution à chaud |
| ~~Le site journalise des erreurs de console sans envoyer un octet de JavaScript~~ | 18/8 → **tranché par la CI le 18/8** | L'hypothèse « c'est la favicon » était **incomplète**, et le test E2E écrit pour la trancher l'a montré. Trois causes en réalité : la favicon absente (corrigée, `public/favicon.svg`), le statut 404 de la page `/404` elle-même (légitime), et surtout **la bannière de deploy preview de Netlify** — un `<div … style="position:fixed">` et une iframe d'`app.netlify.com` que **notre propre CSP bloque**, sur du balisage que personne ici n'a écrit et que la production ne reçoit jamais. La CSP faisait son travail ; c'est la mesure qui lisait mal. Corollaire pour le §5 : le score `errors-in-console` de Lighthouse mesuré sur une preview mesure en partie l'injection de Netlify, pas le site |
| La requête bloquante du rendu est le prix de la CSP, pas un défaut | 18/8 | `render-blocking-insight` à 0,5 sur `/` : la feuille de style est une requête séparée qui bloque le rendu. C'est **voulu** — le §7 interdit `unsafe-inline`, donc `build.inlineStylesheets` vaut `'never'`, donc aucun CSS critique ne peut être mis en ligne. Lighthouse retirera toujours ces points. Écrit ici pour qu'une session future ne « corrige » pas l'audit en relâchant la CSP : ce serait échanger une exigence de sécurité contre un score de performance |
| ~~`/` répond plus lentement que `/stats`, et l'inverse serait attendu~~ | 18/8 → soldé le 19/8 | La ligne disait « ce qu'il faut d'abord est une mesure qui distingue le froid du chaud, pas un chiffre ». Cette mesure existe : le préchauffage du ticket #46 journalise le temps de la **première** réponse de chaque route. Sur la preview de la PR #47, `/` — touchée en premier — met **4 024 ms**, `/stats` **2 492 ms**, et les cinq routes suivantes 140 à 233 ms. L'anomalie n'était donc pas dans `/` : elle était dans l'ordre de passage, et elle disparaît dès que la fonction est réveillée avant qu'on la mesure |
| `script-src 'self'` autorise ce que Netlify sert sous notre origine | 18/8 | **Constaté** en cherchant pourquoi un test échouait : la bannière de deploy preview est ajoutée **après `</html>`** et charge `<script async src="/.netlify/scripts/cdp">` — un chemin de **notre propre origine**, donc permis par la CSP, qui injecte ensuite du balisage à l'exécution. La directive n'est pas violée ; elle est simplement moins étroite qu'elle ne se lit, parce que la plateforme peut servir du script sous notre domaine. Sans conséquence connue — c'est du code Netlify sur des previews — mais à savoir avant de raisonner sur ce que `'self'` garantit, et à revérifier si la production reçoit un jour une injection du même genre |
| ~~`playwright install --with-deps` est une dépendance non bornée sur le chemin critique~~ | 18/8 → soldé le 22/8 | **Mesuré** sur la PR #44 : l'étape a pris **11 min 02** au lieu des ~25 s habituelles, sans échouer ni rien dire. Le job a fini à 11 min 44, à trois minutes de son propre `timeout-minutes: 15`, et la suite elle-même n'a duré que 21 s. La cible du §5 — moins de dix minutes du push au verdict — a donc été crevée par une installation de paquets système, pas par des tests. La ligne disait « à traiter si le motif se répète — la fréquence est ce qui manque pour trancher » : il s'est répété le 19/8 en tuant deux jobs, la fréquence est mesurée (trois occurrences sur quatre exécutions), et les deux pistes qu'elle énumérait sans les essayer sont appliquées par le ticket #48 — plafond propre à l'étape et retrait de `--with-deps`. La troisième, mettre le navigateur en cache, n'a pas été prise : sans `apt` l'étape ne fait plus qu'un téléchargement, et un cache est une deuxième chose à invalider (cf. la ligne suivante) |
| Le téléchargement du navigateur reste une dépendance réseau sur le chemin critique | 22/8 | Ce que le ticket #48 laisse : `playwright install chromium` ne touche plus `apt`, mais il tire toujours ~170 Mo depuis les serveurs de Playwright dans `e2e` **et** dans `lighthouse`, à chaque exécution. La différence avec la dette qu'il solde est que cette dépendance est désormais **bornée et nommée** — cinq minutes, sous le nom de l'étape. Sortie possible si le motif se reproduit : mettre en cache `~/.cache/ms-playwright` sur la clé du lockfile. Pas prise ici parce qu'un cache est une deuxième chose à invalider, et parce que la mesure la retire de l'urgence : sans `apt`, l'étape tient en **10 à 11 s** (PR #49, quatre mesures), soit un trentième de son plafond. À reposer si un téléchargement s'en approche |
| Le budget Lighthouse mesure une preview que Netlify modifie | 18/8 | Conséquence directe de la ligne ci-dessus : la bannière de deploy preview ajoute du balisage, une iframe bloquée et deux violations de CSP à **chaque** page mesurée. Les scores `best-practices` relevés en PR sont donc plafonnés par du tiers. Deux issues, aucune gratuite : mesurer la production au lieu de la preview — mais alors le budget ne garde plus un diff avant son merge —, ou soustraire explicitement ces audits. À trancher quand le plafond gênera, pas avant |
| Le budget Lighthouse ne couvre pas les pages de données à venir | 18/8 | Deux pages mesurées, un gabarit chacune, comme le §5 le veut de l'E2E. Les classements du jalon 4 introduiront un gabarit dont le poids dépend des **données** et non du code : c'est celui qui cassera un budget sans qu'aucun diff n'ait l'air coupable. À reprendre avec ce gabarit, pas avant |
| 15 communes du périmètre sans aucune URL | 7/8 | **Mesuré** par l'ingestion : `02810`, `49126`, `97129`, `97220`, `97230`, `97307`, `97608`, `97616`, `97801`, `98712`, `98714`, `98733`, `98734`, `98747`, `98817`. Deux n'ont aucune fiche mairie dans l'annuaire, les treize autres en ont une sans site déclaré. Elles resteront non mesurables tant que la source `manuel` ou `heuristique` n'existe pas. Le job les **nomme** à chaque exécution plutôt que de les compter, pour que la liste ne devienne pas un chiffre |
| Une page derrière un mur JavaScript n'est pas mesurable | 23/8 | **Constaté** sur le relevé de J2-03 : une commune sur 45 répond `200` avec 216 octets — un script qui pose un cookie et recharge, et un `<noscript>` qui refuse l'accès. Le brief exclut le navigateur piloté (§4), donc ce mur ne sera pas franchi ; le module refuse un document sans aucun lien (`empty-document`) et la mesure porte un échec plutôt qu'une absence. La conséquence à tenir est ailleurs : cette commune restera indéfiniment en échec définitif, donc la surface d'ops (J2-06) doit pouvoir montrer cette file et la distinguer d'un site en panne |
| Le CMS de 7 communes sur 41 n'est pas identifié | 23/8 | La table d'empreintes couvre les cinq produits que le relevé a vus (WordPress 22, TYPO3 8, Drupal 2, Joomla 1, SPIP 1). Restent deux produits nommés mais inconnus — `abc-cms`, `Moovapps Commerce Studio` — et cinq sites sans aucune empreinte. `null` veut donc dire « aucune empreinte reconnue » et non « pas de CMS », ce que la page qui publiera le signal devra dire aussi. La table grandit sur mesure, jamais sur catalogue : une empreinte copiée d'un détecteur générique est une occasion de faux positif que personne n'ira vérifier |
| `has_csp` ne dit pas ce que la politique protège | 23/8 | **Mesuré** : 15 des 41 pages déclarent une CSP, dont **6 ne posent que `frame-ancestors`**. Le booléen dit « une politique est déclarée », pas « une bonne politique ». Le graduer serait un morceau de score composite, décision explicitement ouverte (`brief.md` §11) qu'un module d'extraction n'a pas à trancher au passage. À reprendre avec la formule du score, pas avant |
| Le vocabulaire des signaux n'est confronté à rien après la PR | 23/8 | Les libellés et chemins de pied de page sont gelés dans des tables de tests unitaires, et **rien ne les rejoue contre le vrai web** : `tests/contract/` interroge des API, pas les sites des communes, et le §5 interdit toute requête réelle sur le chemin d'une PR. Une mode de pied de page qui changerait passerait donc inaperçue. La prochaine confrontation est le premier scan réel (J2-05), qui produira les mêmes taux sur plus de mille communes au lieu de 41 : un effondrement d'un de ces taux se lira comme une dérive du vocabulaire, pas comme une régression des communes |
