/**
 * Drizzle schema — the five tables of docs/brief.md §6 (commune, site,
 * scan_run, measurement, finding) land here in J1-08.
 *
 * The file exists at bootstrap so that `pnpm db:generate` and `pnpm db:check`
 * are runnable commands rather than broken ones.
 */
export {};
